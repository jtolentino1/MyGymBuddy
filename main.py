import json
import Curls
import Squats
# import Squats

def main():
    # routine = ["curls", "squats", "extensions"]
    
    # f = open("exercises.json")
    # data = json.load(f)
    # for i in routine:
    #     joints = data["exercises"][i]["joints"]
    #     angle = data["exercises"][i]["angles"]
    #    # print(joints,angle)
    #importCurls
    Curls.start()


if __name__ == "__main__":
    main()